# Raízes 360 — API

Back-end do sistema Raízes 360. Node.js + Express + Supabase.

## Setup

1. Copie `.env.example` para `.env` e preencha as variáveis
2. `npm install`
3. `npm start`

Ver DEPLOY.md para o guia completo de hospedagem gratuita.
